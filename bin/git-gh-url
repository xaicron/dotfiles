#!/usr/bin/env perl

use strict;
use warnings;
use feature 'say';

chomp(my $remote = `git config remote.origin.url`);
exit $? if $?;

$remote =~ s!:!/!;
$remote =~ s!git\@!https://!;
$remote =~ s!\.git$!!;

say $remote;
